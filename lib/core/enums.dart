enum ScreenType { about, experience, projects, work, contact }

enum ScreenSize { web, mobile, tab, work, contact }

enum Technology {
  android,
  flutter,
  shopify,
  javascript,
  ios,
  web,
  graphql,
  firebase,
  wordpress,
  typescript,
  ionic,
  opencart,
  angular,
  php,
}
